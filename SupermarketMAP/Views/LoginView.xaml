<Window x:Class="SupermarketMAP.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login" Height="300" Width="400">
    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock Text="Username" Margin="10"/>
            <TextBox Text="{Binding usernameP}" Width="200" Margin="10"/>

            <TextBlock Text="Password" Margin="10"/>
            <TextBox Text="{Binding passwordP, Mode=TwoWay}" Width="200" Margin="10"/>

            <Button Content="Login" Command="{Binding LoginCommand}" Width="200" Margin="10" />
        </StackPanel>
    </Grid>
</Window>
